---
phase: 03-attachments-and-notifications
plan: 05
type: execute
wave: 2
depends_on: [03-04]
files_modified:
  - landing-page-new/src/lib/attachments.ts
  - landing-page-new/src/lib/portal/components/UploadFileModal.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Client uses correct R2 presign API path"
    - "File uploads limited to 10MB"
    - "Upload modal shows error for files > 10MB"
  artifacts:
    - path: "landing-page-new/src/lib/attachments.ts"
      provides: "Client-side attachment logic"
  key_links:
    - from: "landing-page-new/src/lib/attachments.ts"
      to: "/.netlify/functions/r2-presign"
      via: "fetch"
---

<objective>
Fix frontend integration issues including incorrect API paths and validation mismatches.
Align client portal with backend capabilities and requirements.

Purpose: Enable users to upload files in the client portal by hitting the correct endpoint and enforcing correct limits.
Output: Updated frontend code that communicates correctly with the fixed backend.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-attachments-and-notifications/03-UAT.md
@landing-page-new/src/lib/attachments.ts
@landing-page-new/src/lib/portal/components/UploadFileModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix API Path in Attachments Lib</name>
  <files>landing-page-new/src/lib/attachments.ts</files>
  <action>
    Update `landing-page-new/src/lib/attachments.ts`:
    1. Locate `getPresignedUploadUrl` function.
    2. Change the fetch URL from `${API_BASE}/api/r2-presign` to `${API_BASE}/r2-presign`.
    3. The backend function is deployed at `/.netlify/functions/r2-presign`, not `.../api/r2-presign`.
    4. Note: `API_BASE` defaults to `/.netlify/functions`, so the result should be `/.netlify/functions/r2-presign`.
  </action>
  <verify>
    grep "r2-presign" landing-page-new/src/lib/attachments.ts
  </verify>
  <done>
    API path is correct.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix File Validation Limits</name>
  <files>landing-page-new/src/lib/portal/components/UploadFileModal.tsx</files>
  <action>
    Update `landing-page-new/src/lib/portal/components/UploadFileModal.tsx`:
    1. Change `MAX_FILE_SIZE` from 500MB (`500 * 1024 * 1024`) to 10MB (`10 * 1024 * 1024`) to match backend limits and UAT expectations.
    2. Update the error message to reflect the 10MB limit.
    3. Add `accept` attribute to the file input to restrict file types (PNG, JPG, PDF, DOCX, TXT) consistent with backend allowlist.
    4. Ensure validation logic uses the new limit.
  </action>
  <verify>
    grep "MAX_FILE_SIZE" landing-page-new/src/lib/portal/components/UploadFileModal.tsx
  </verify>
  <done>
    Validation limit is 10MB and file types are restricted.
  </done>
</task>

</tasks>

<verification>
Check API paths and validation constants.
</verification>

<success_criteria>
- [ ] Client fetches correct URL for presign
- [ ] File size limit is 10MB
- [ ] File input has accept attribute
</success_criteria>

<output>
After completion, create `.planning/phases/03-attachments-and-notifications/03-05-SUMMARY.md`
</output>

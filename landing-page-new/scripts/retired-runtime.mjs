#!/usr/bin/env node

console.error([
  'landing-page-new is retired and is kept only as non-runtime legacy reference.',
  'Use the root Vite app for browser routes: npm run dev, npm run build, or npm run dev:all.',
].join('\n'));

process.exit(1);
